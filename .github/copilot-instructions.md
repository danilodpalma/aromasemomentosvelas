# Workspace instructions

- Project type: Next.js + TypeScript + Prisma + SQLite
- Purpose: Inventory and sales tracker for products, costs, prices, and orders.
- Start with `npm install`, then `npx prisma generate` and `npx prisma db push`.
- Run with `npm run dev`.
